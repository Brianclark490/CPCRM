{
  "name": "@cpcrm/config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'no build step for config package'",
    "lint": "echo 'no lint step for config package'"
  }
}
